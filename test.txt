/* this is
a big comment
block!
*/
var start = clock();
fun fib(n) {
  if (n <= 1) return n;
  return fib(n - 1) + fib(n - 2);
}

for (var i = 0; i < 20; i = i + 1) {
  fib(i);
}
var end = clock();
var timeTaken = end-start;
print("Took ");
print(timeTaken);
print("ms");